# Railway deployment configuration for Legal Research Tool

[build]
builder = "nixpacks"

[deploy]
startCommand = ". /opt/venv/bin/activate && cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100