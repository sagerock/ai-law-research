# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["python311", "postgresql15"]

[phases.install]
cmds = [
    "pip install -r backend/requirements.txt"
]

[start]
cmd = "cd backend && uvicorn simple_api:app --host 0.0.0.0 --port ${PORT:-8000}"